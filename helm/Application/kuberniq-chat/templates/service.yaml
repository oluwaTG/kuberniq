apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-chatbot.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mcp-chatbot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcp-chatbot.selectorLabels" . | nindent 4 }}
